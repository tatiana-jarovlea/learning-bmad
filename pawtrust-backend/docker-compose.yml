services:
  db:
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: pawtrust
      MYSQL_ROOT_PASSWORD: secret
    ports:
      - "3306:3306"
    volumes:
      - db_data:/var/lib/mysql
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
volumes:
  db_data:
