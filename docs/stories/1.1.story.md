# Story 1.1: Project Scaffolding & AWS Environment Setup

**Epic:** 1 — Foundation & Authentication
**Status:** Done

---

## Story

**As a** developer,
**I want** a working local dev environment and AWS staging environment,
**so that** I can build and test all subsequent features in a production-like context.

---

## Acceptance Criteria

1. `pawtrust-frontend` repo: React 18 + TypeScript + TailwindCSS project initialized with React Router and Axios. `npm run dev` serves locally on port 5173.
2. `pawtrust-backend` repo: Laravel 11 + PHP 8.2 project initialized with Laravel Sanctum and Laravel Queue configured. `php artisan serve` runs locally on port 8000.
3. Docker Compose file in `pawtrust-backend` starts MySQL 8 + Redis containers. `docker-compose up` results in working local database. PHP runs natively via `php artisan serve` — not containerised.
4. AWS staging environment provisioned: EC2/Elastic Beanstalk, RDS MySQL 8 instance, S3 bucket (private), SES verified domain, CloudFront distribution. All connected and accessible from deployed Laravel app.
5. `.env.example` files in both repos document all required environment variables. No secrets committed to version control.
6. `README.md` in both repos documents local setup steps, env var requirements, and how to run tests.
7. HTTPS enforced on staging (SSL certificate via ACM).

---

## Tasks / Subtasks

### Backend — `pawtrust-backend`

- [ ] **T1** — Initialize Laravel 11 project
  - `composer create-project laravel/laravel pawtrust-backend`
  - PHP version lock: require PHP `^8.2` in `composer.json`
  - Install Laravel Sanctum: `composer require laravel/sanctum`
  - Run `php artisan sanctum:install`
  - Install Spatie Permission: `composer require spatie/laravel-permission`
  - Publish Spatie migrations: `php artisan vendor:publish --provider="Spatie\Permission\PermissionServiceProvider"`
  - Install PHP CS Fixer: `composer require --dev friendsofphp/php-cs-fixer`
  - Configure `.php-cs-fixer.php` for PSR-12
  - Add `"fix": "vendor/bin/php-cs-fixer fix"` to `composer.json` scripts
  - Create `routes/api.php` health route: `Route::get('/v1/health', fn() => response()->json(['status' => 'ok']));` — no auth, confirms app is running

- [ ] **T2** — Configure Docker Compose
  - Create `docker-compose.yml` in repo root (see Dev Notes §3 for exact content)
  - MySQL 8 service on port 3306 with persistent volume
  - Redis service on port 6379 (for future queue use)
  - Add `.env` values: `DB_HOST=127.0.0.1`, `DB_PORT=3306`, `DB_DATABASE=pawtrust`, `DB_USERNAME=root`, `DB_PASSWORD=secret`
  - Verify: `docker compose up -d` → `php artisan migrate` succeeds

- [ ] **T3** — Run initial migrations
  - Sanctum migration: `personal_access_tokens` table (auto-published by `sanctum:install`)
  - Spatie migrations: `roles`, `permissions`, `model_has_roles`, `model_has_permissions`, `role_has_permissions` tables
  - Verify all tables created on `docker compose up && php artisan migrate`

- [ ] **T4** — Configure Laravel Queue
  - Set `QUEUE_CONNECTION=database` in `.env.example`
  - Create jobs table: `php artisan queue:table && php artisan migrate`
  - Create failed jobs table: `php artisan make:queue-failed-table && php artisan migrate`
  - Verify: `php artisan queue:work` starts without error

- [ ] **T5** — Configure S3 storage driver
  - Install AWS SDK: `composer require league/flysystem-aws-s3-v3`
  - Configure `config/filesystems.php` s3 disk with env vars
  - Add to `.env.example`: `AWS_ACCESS_KEY_ID`, `AWS_SECRET_ACCESS_KEY`, `AWS_DEFAULT_REGION=eu-central-1`, `AWS_BUCKET`, `AWS_CLOUDFRONT_URL`
  - Verify: `Storage::disk('s3')->put('test.txt', 'hello')` succeeds in tinker (with real staging creds)

- [ ] **T6** — Configure SES mail driver
  - Install: `composer require aws/aws-sdk-php`
  - In `config/mail.php`, set default mailer configurable via `MAIL_MAILER`
  - `.env.example`: `MAIL_MAILER=log` (local) / `MAIL_MAILER=ses` (staging/prod)
  - Add `AWS_ACCESS_KEY_ID` and `AWS_SECRET_ACCESS_KEY` (shared with S3)

- [ ] **T7** — Create `.env.example` (complete)
  - All required keys listed (see Dev Notes §4 for full list)
  - No actual secret values — only comments describing expected values
  - Include: `SKIP_LISTING_PAYMENT=true`, `TELESCOPE_ENABLED=true`, `ADMIN_EMAIL`, `ADMIN_PASSWORD`

- [ ] **T8** — Write `README.md`
  - Prerequisites (Docker, PHP 8.2, Composer 2.x)
  - Local setup steps (exact commands from deployment.md)
  - How to run tests: `php artisan test`
  - How to run code fixer: `composer fix`
  - Env var descriptions (reference `.env.example`)

- [ ] **T9** — Set up GitHub Actions CI
  - Create `.github/workflows/ci.yml` (see Dev Notes §5)
  - Jobs: composer install → PHP CS Fixer dry-run → PHPUnit
  - Verify: dummy PR triggers workflow and passes

### Frontend — `pawtrust-frontend`

- [ ] **T10** — Initialize Vite + React + TypeScript project
  - `npm create vite@latest pawtrust-frontend -- --template react-ts`
  - Install TailwindCSS: `npm install -D tailwindcss postcss autoprefixer && npx tailwindcss init -p`
  - Configure `tailwind.config.ts` with `content: ["./src/**/*.{ts,tsx}"]`
  - Install React Router: `npm install react-router-dom@6`
  - Install Axios: `npm install axios`
  - Install TanStack Query: `npm install @tanstack/react-query`
  - Install Zustand: `npm install zustand`
  - Install react-i18next: `npm install react-i18next i18next`
  - Install React Helmet Async: `npm install react-helmet-async`
  - Verify: `npm run dev` serves on port 5173

- [ ] **T11** — Configure TypeScript paths (absolute imports)
  - In `tsconfig.json`, add: `"paths": { "@/*": ["./src/*"] }`
  - In `vite.config.ts`, add resolve alias: `'@': '/src'`
  - Verify: `import { foo } from '@/utils/foo'` resolves correctly

- [ ] **T12** — Configure ESLint + Prettier
  - Install: `npm install -D eslint prettier eslint-config-airbnb-typescript eslint-plugin-react eslint-plugin-react-hooks @typescript-eslint/parser @typescript-eslint/eslint-plugin`
  - Create `.eslintrc.cjs` and `.prettierrc`
  - Add to `package.json` scripts: `"lint": "eslint src --ext .ts,.tsx"`, `"lint:fix": "eslint src --ext .ts,.tsx --fix"`
  - Verify: `npm run lint` passes on fresh project

- [ ] **T13** — Create base directory structure
  - Create empty directories: `src/api/`, `src/components/common/`, `src/components/layout/`, `src/components/listings/`, `src/pages/`, `src/hooks/`, `src/store/`, `src/types/`, `src/utils/`
  - Create `src/i18n.ts` (i18next initialization)
  - Create `public/locales/ro/translation.json` and `public/locales/ru/translation.json` (empty objects `{}`)
  - Create `src/router.tsx` (minimal — just home route)
  - Verify: `npm run build` succeeds with empty structure

- [ ] **T14** — Create Axios client
  - Create `src/api/axiosClient.ts`
  - Configure: `baseURL` from `import.meta.env.VITE_API_BASE_URL`
  - Add request interceptor: attach `Authorization: Bearer {token}` from Zustand auth store
  - Add response interceptor: on 401, clear auth store → redirect to `/login`

- [ ] **T15** — Create minimal auth store (Zustand)
  - Create `src/store/authStore.ts`
  - State: `{ token: string | null, user: User | null, locale: 'ro' | 'ru' }`
  - Actions: `setAuth(token, user)`, `clearAuth()`, `setLocale(locale)`
  - Persist to `localStorage` via Zustand persist middleware
  - Create `src/types/user.ts` with `User` interface: `{ id, name, email, role, locale }`

- [ ] **T16** — Create `.env.example`
  - `VITE_API_BASE_URL=http://localhost:8000/api/v1`
  - `VITE_STRIPE_PUBLISHABLE_KEY=`
  - `VITE_APP_ENV=development`

- [ ] **T17** — Write `README.md`
  - Prerequisites (Node.js 20+)
  - Local setup steps
  - How to run tests: `npm run test`
  - How to run lint: `npm run lint`

- [ ] **T18** — Set up GitHub Actions CI
  - Create `.github/workflows/ci.yml` (see Dev Notes §5)
  - Jobs: npm install → lint → type-check → test:ci
  - Verify: dummy PR triggers workflow and passes

### AWS Staging Environment

- [ ] **T19** — Provision AWS resources (staging)
  - **RDS:** MySQL 8.0 instance (`db.t3.micro`), VPC private subnet, security group allows EB only
  - **S3 (private files):** Bucket `pawtrust-private-staging`, Block All Public Access enabled
  - **S3 (frontend):** Bucket `pawtrust-frontend-staging`, static website disabled (CloudFront only)
  - **CloudFront (frontend):** Distribution with S3 origin, OAC policy, HTTPS only, custom domain if available
  - **SES:** Verify domain `pawtrust.md` (DKIM + SPF + DMARC DNS records)
  - **ACM:** Request wildcard cert `*.pawtrust.md` in `us-east-1` (for CloudFront) AND `eu-central-1` (for EB)
  - **EB:** Create PHP 8.2 environment, attach RDS, configure env vars from `.env.example`

- [ ] **T20** — Verify staging connectivity
  - Deploy Laravel app to EB with `php artisan migrate`
  - Test S3 upload via tinker
  - Test SES send via `php artisan tinker` → `Mail::raw('test', fn($m) => $m->to('test@example.com')->subject('test'))`
  - Confirm HTTPS working via ACM cert

---

## Dev Notes

### §1 — Architecture Decisions

This story creates the foundation. Key constraints from architecture docs:

> **Port clarification:** AC#1 specifies port 5173. The parent Epic 1.1 states port 3000 — that value is a CRA-era artefact. Vite's actual default is 5173 and this story is correct. [Supersedes Epic 1.1 AC#1]

> **Docker Compose scope:** Docker Compose provides MySQL 8 + Redis only. PHP does not run inside a container at development time — developers run `php artisan serve` natively. The Epic's AC#3 wording ("starts PHP app + MySQL 8 container") is imprecise; this story's approach (MySQL + Redis via Docker, PHP native) is the agreed implementation.

- **Polyrepo:** two separate Git repos. No monorepo. [Source: architecture/unified-project-structure.md]
- **API base path:** All Laravel routes under `/api/v1/`. Vite env var `VITE_API_BASE_URL` points to this base. [Source: architecture/rest-api-spec.md]
- **Auth storage:** Token in Zustand store + persisted to localStorage. Axios interceptor attaches to all requests. On 401, clear store + redirect to `/login`. [Source: architecture/frontend-architecture.md]
- **Queue:** Use database driver at MVP (`QUEUE_CONNECTION=database`). Redis not required until production volume demands it. [Source: architecture/tech-stack.md]

### §2 — File Naming & Import Convention

- All PHP classes: `PascalCase.php`, snake_case column names in DB
- All TypeScript components: `PascalCase.tsx`, hooks: `use{Name}.ts`
- Absolute imports: `@/api/axiosClient` not `../../api/axiosClient`
- [Source: architecture/coding-standards.md]

### §3 — Docker Compose (exact content)

```yaml
# pawtrust-backend/docker-compose.yml
services:
  db:
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: pawtrust
      MYSQL_ROOT_PASSWORD: secret
    ports:
      - '3306:3306'
    volumes:
      - db_data:/var/lib/mysql
  redis:
    image: redis:7-alpine
    ports:
      - '6379:6379'
volumes:
  db_data:
```

### §4 — Complete `.env.example` for Backend

```dotenv
APP_NAME=PawTrust
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:8000

LOG_CHANNEL=stack
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=pawtrust
DB_USERNAME=root
DB_PASSWORD=secret

BROADCAST_DRIVER=log
CACHE_DRIVER=file
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database
SESSION_DRIVER=file

MAIL_MAILER=log
MAIL_HOST=mailhog
MAIL_PORT=1025
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="noreply@pawtrust.md"
MAIL_FROM_NAME="${APP_NAME}"

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=eu-central-1
AWS_BUCKET=pawtrust-private-local
AWS_CLOUDFRONT_URL=

STRIPE_KEY=
STRIPE_SECRET=
STRIPE_WEBHOOK_SECRET=

SANCTUM_STATEFUL_DOMAINS=localhost:5173,127.0.0.1:5173

ADMIN_EMAIL=admin@pawtrust.md
ADMIN_PASSWORD=changeme_in_env
ADMIN_NOTIFICATION_EMAIL=team@pawtrust.md

SKIP_LISTING_PAYMENT=true
TELESCOPE_ENABLED=true
```

### §5 — GitHub Actions CI (both repos)

**pawtrust-backend `.github/workflows/ci.yml`:**

```yaml
name: CI
on: [pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_DATABASE: pawtrust_test
          MYSQL_ROOT_PASSWORD: secret
        ports:
          - 3306:3306
    steps:
      - uses: actions/checkout@v4
      - uses: shivammathur/setup-php@v2
        with: { php-version: '8.2' }
      - run: composer install --prefer-dist --no-progress
      - run: cp .env.example .env && php artisan key:generate
      - run: vendor/bin/php-cs-fixer fix --dry-run --diff
      - run: php artisan test
        env:
          DB_HOST: 127.0.0.1
          DB_DATABASE: pawtrust_test
          DB_USERNAME: root
          DB_PASSWORD: secret
```

**pawtrust-frontend `.github/workflows/ci.yml`:**

```yaml
name: CI
on: [pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: npm ci
      - run: npm run lint
      - run: npx tsc --noEmit
      - run: npm run test:ci
```

### §6 — What This Story Does NOT Include

The following are explicitly out of scope for Story 1.1 and handled in later stories:

- User registration/login endpoints (Story 1.2, 1.3)
- Database tables beyond Sanctum + Spatie (Story 1.2+)
- Business logic of any kind
- Any React pages beyond the empty shell + router
- Stripe integration (Story 5.3)

---

## Testing

### Backend

- **Unit:** N/A for this story (no business logic)
- **Feature:** Verify `php artisan test` runs and passes on CI-like environment
- **Manual:** `docker compose up -d && php artisan migrate` succeeds with 0 errors

### Frontend

- **Component:** N/A for this story
- **Manual:** `npm run dev` serves on :5173; `npm run build` succeeds; `npm run lint` passes

### Smoke Check (Definition of Done)

```bash
# Backend
docker compose up -d
composer install
php artisan key:generate
php artisan migrate
php artisan serve &
curl http://localhost:8000/api/v1/health  # → {"status":"ok"} (add health endpoint)

# Frontend
npm install
npm run build  # → no TypeScript or lint errors
```

---

## Change Log

| Date       | Version | Author         | Change        |
| ---------- | ------- | -------------- | ------------- |
| 2026-03-20 | 1.0     | Bob (SM Agent) | Initial draft |

